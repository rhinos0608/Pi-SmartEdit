# Hashline vs Legacy Benchmark Report

**Model**: anthropic/claude-sonnet-4-6  
**Runs per task**: 3  
**Tasks**: 20  
**Date**: 2026-05-01

## Success Rate

| Mode     | Success Rate | Avg Duration | Est. Output Tokens |
|----------|-------------|-------------|---------------------|
| Hashline | 0% | 4ms | 4 |
| Legacy   | 44.4% | TBD | 15 |

## Hashline Tier Distribution

| Tier     | Count | Pct |
|----------|-------|-----|
| hashline | 27 | 100% |

## Per-Task Results

| Task | Hashline | Legacy |
|------|----------|--------|
| single-line-replace | 0/3 | 3/3 |
| multi-line-replace | 0/3 | 0/3 |
| delete-range | 0/3 | 0/3 |
| function-body-replace | 0/3 | 0/3 |
| stale-file-rejection | 0/3 | 3/3 |
| append-at-end | 0/3 | 0/3 |
| prepend-at-start | 0/3 | 0/3 |
| syntax-correction | 0/3 | 3/3 |
| indentation-sensitive | 0/3 | 3/3 |

## Errors

- **single-line-replace** (hashline, run 0): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **single-line-replace** (hashline, run 1): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **single-line-replace** (hashline, run 2): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **multi-line-replace** (hashline, run 0): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **multi-line-replace** (legacy, run 0): Output mismatch. Got: function getUser(id: string) {
  const user = db.find(id);
  return {
    name: user.name,
  };
}
  };
}
- **multi-line-replace** (hashline, run 1): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **multi-line-replace** (legacy, run 1): Output mismatch. Got: function getUser(id: string) {
  const user = db.find(id);
  return {
    name: user.name,
  };
}
  };
}
- **multi-line-replace** (hashline, run 2): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **multi-line-replace** (legacy, run 2): Output mismatch. Got: function getUser(id: string) {
  const user = db.find(id);
  return {
    name: user.name,
  };
}
  };
}
- **delete-range** (hashline, run 0): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **delete-range** (legacy, run 0): Output mismatch. Got: 
return process(body);
console.log('DEBUG: auth header:', auth);

return process(body);
- **delete-range** (hashline, run 1): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **delete-range** (legacy, run 1): Output mismatch. Got: 
return process(body);
console.log('DEBUG: auth header:', auth);

return process(body);
- **delete-range** (hashline, run 2): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **delete-range** (legacy, run 2): Output mismatch. Got: 
return process(body);
console.log('DEBUG: auth header:', auth);

return process(body);
- **function-body-replace** (hashline, run 0): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **function-body-replace** (legacy, run 0): Output mismatch. Got: class UserService {
  async createUser(data: CreateUserInput) {
    const validated = validate(data);
    const user = await this.repo.save(validated);
    return user;
    return user;
  }
}
- **function-body-replace** (hashline, run 1): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **function-body-replace** (legacy, run 1): Output mismatch. Got: class UserService {
  async createUser(data: CreateUserInput) {
    const validated = validate(data);
    const user = await this.repo.save(validated);
    return user;
    return user;
  }
}
- **function-body-replace** (hashline, run 2): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **function-body-replace** (legacy, run 2): Output mismatch. Got: class UserService {
  async createUser(data: CreateUserInput) {
    const validated = validate(data);
    const user = await this.repo.save(validated);
    return user;
    return user;
  }
}
- **stale-file-rejection** (hashline, run 0): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **stale-file-rejection** (hashline, run 1): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **stale-file-rejection** (hashline, run 2): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **append-at-end** (hashline, run 0): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **append-at-end** (legacy, run 0): Output mismatch. Got: import { foo } from './foo';
import { bar } from './bar';
- **append-at-end** (hashline, run 1): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **append-at-end** (legacy, run 1): Output mismatch. Got: import { foo } from './foo';
import { bar } from './bar';
- **append-at-end** (hashline, run 2): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **append-at-end** (legacy, run 2): Output mismatch. Got: import { foo } from './foo';
import { bar } from './bar';
- **prepend-at-start** (hashline, run 0): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **prepend-at-start** (legacy, run 0): Output mismatch. Got: // Copyright 2026
import { bar } from './bar';
- **prepend-at-start** (hashline, run 1): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **prepend-at-start** (legacy, run 1): Output mismatch. Got: // Copyright 2026
import { bar } from './bar';
- **prepend-at-start** (hashline, run 2): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **prepend-at-start** (legacy, run 2): Output mismatch. Got: // Copyright 2026
import { bar } from './bar';
- **syntax-correction** (hashline, run 0): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **syntax-correction** (hashline, run 1): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **syntax-correction** (hashline, run 2): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **indentation-sensitive** (hashline, run 0): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **indentation-sensitive** (hashline, run 1): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.
- **indentation-sensitive** (hashline, run 2): Error: xxHash32 not initialized. Call computeLineHash() once (async init) or ensureXXHash32() before using the sync variant.