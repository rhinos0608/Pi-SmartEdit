/**
 * Shared constants for the GumTree-Simplified structural diff.
 *
 * Extracted verbatim from structural-diff.ts (values unchanged).
 * No match logic lives here.
 */

export const MAX_TOTAL_NODES = 5000;
export const MAX_BOTTOM_UP_UNMATCHED = 500;
export const MATCH_CANDIDATE_LIMIT = 20;

export const COMMENT_TYPE_RE = /^comment$/i;
